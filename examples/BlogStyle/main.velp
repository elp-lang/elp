import { App, Window } from "elp"
import { Home } from "$screens/home"
import Styles from "$app/styles"

export fn HelloWorld -> App {
	return App(globalStyles = Styles()) {
		Window {
			Home()
		}
	}
}
