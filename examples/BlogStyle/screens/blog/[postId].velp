import { Uuid } from elp.types
import { Screen } from elp.app
import { Column, Row, Text } from elp.ui

import { Paragraph } from .components.paragraph

object BlogPostParams {
    postId string
}

@Screen<BlogPostParams>
export fn BlogPost({ params }) {
	const post, error = getBlogPost(params.postId)

    Column {
        if error not None {
            Row(class = "error-row") {
                Text(error.asString())
            }
        } else {
            Row {
                Text(post.title, class = "title")
                Paragraph(post.body)
            }
        }
    }
}

// vim: ft=kotlin
